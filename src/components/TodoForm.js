import React from 'react'

export default props =>
    <form>
        <input
            type="text"
            autofocus
            className="new-todo"
            placeholder="What needs to be done?"
        />
    </form>